<?xml version="1.0" encoding="UTF-8"?>

<web-app xmlns="http://java.sun.com/xml/ns/javaee" version="3.0">

    <servlet>
        <servlet-name>action</servlet-name>
        <!-- servlet-class : 우리가 라이브러리에 넣은 spring-webmvc.jar 안에 실제 존재하는 자바 클랫 ㅡ경로 -->
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
<!--         <init-param>
            <param-name>contextConfigLocation</param-name>
            param-value : 참고할 세부 지침서(action-servlet.xml)가 어디에 있는지 지정
            <param-value>/WEB-INF/action-servlet.xml</param-value>
        </init-param> -->
    </servlet>

    <servlet-mapping>
        <servlet-name>action</servlet-name>
        <!-- url-pattern : 브라우저 주소창에 abc.do 처럼 끝에 .do가 붙은 모든 요청은 스프링이 관리하겠다. -->
        <url-pattern>*.do</url-pattern>
    </servlet-mapping>

</web-app>